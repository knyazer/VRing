#version 330 core

// Interpolated values from the vertex shaders
in vec3 fragmentColor;
in vec3 vBC;

// Ouput data
out vec3 color;

void main()
{
	if (any(lessThan(vBC.yz, vec2(0.03))))
	{
		color = vec3(0.7);//(vBC + vec3(1, 1, 1)) * 0.5;
	}
	else
	{
	color = fragmentColor;
	}
	
}